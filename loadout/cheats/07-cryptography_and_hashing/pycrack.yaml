tool: pycrack
tags:
- m07
- crypto
- hash
exam_hint: 'PyCrack is a Python-based password cracking utility used for offline cracking of password hashes using dictionary and brute-force attacks. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Pycrack --help
  desc: Runs `pycrack` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: pycrack --help
- title: Man pycrack
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man pycrack
