tool: billcipher
tags:
- m07
- crypto
- hash
exam_hint: 'BillCipher is a password recovery and decryption tool for Windows, supporting the extraction and decryption of saved passwords from browsers and applications. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://github.com/GiovanniBussi/BillCipher
- https://www.kali.org/tools/billcipher/
actions:
- title: Billcipher --help
  desc: Runs `billcipher` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: billcipher --help
- title: Man billcipher
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man billcipher
