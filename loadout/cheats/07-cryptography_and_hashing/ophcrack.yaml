tool: ophcrack
tags:
- m07
- crypto
- hash
exam_hint: 'Ophcrack is a free Windows password cracker based on rainbow tables. It is a very efficient implementation of rainbow tables done by the inventors of the method. It cracks LM and NTLM hashes. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://github.com/nobutaka/ophcrack.git
actions:
- title: Apt-get install
  desc: Runs `sudo` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: sudo apt-get install ophcrack ophcrack-wordlists
- title: Git clone https://github.com/nobutaka/ophcrack.git
  desc: Runs `git` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: git clone https://github.com/nobutaka/ophcrack.git
- title: Cd ophcrack
  desc: Runs `cd` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cd ophcrack
- title: Configure
  desc: Runs `./configure` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: ./configure
- title: Make
  desc: Runs `make` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: make
- title: Make install
  desc: Runs `sudo` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: sudo make install
- title: Download ISO from
  desc: Runs `Download` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: Download ISO from ophcrack website
- title: Boot from USB/DVD
  desc: Runs `Boot` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: Boot from USB/DVD
- title: Pwdump.exe > hashes.txt
  desc: Runs `pwdump.exe` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: pwdump.exe > hashes.txt
- title: Samdump2 SYSTEM SAM
  desc: Runs `samdump2` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: samdump2 SYSTEM SAM > hashes.txt
- title: 1. Load →
  desc: Runs `1.` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: 1. Load → PWDUMP file
