tool: cryptoforge
tags:
- m07
- crypto
- hash
exam_hint: 'CryptoForge is a file encryption software for Windows, providing strong encryption for files and folders using multiple algorithms. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://www.cryptoforge.com/
- https://en.wikipedia.org/wiki/File_encryption
actions:
- title: Cryptoforge --help
  desc: Runs `cryptoforge` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cryptoforge --help
- title: Man cryptoforge
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man cryptoforge
