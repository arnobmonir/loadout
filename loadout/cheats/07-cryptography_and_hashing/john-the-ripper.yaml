tool: john-the-ripper
tags:
- m07
- crypto
- hash
exam_hint: 'John the Ripper is a popular password cracking tool supporting a wide range of hash algorithms and attack modes, widely used in penetration testing and digital forensics. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://www.openwall.com/john/
- https://en.wikipedia.org/wiki/John_the_Ripper
actions:
- title: John --wordlist=<wordlist> <hashfile>
  desc: Runs `john` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: john --wordlist=<wordlist> <hashfile>
- title: John --show <hashfile>
  desc: Runs `john` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: john --show <hashfile>
