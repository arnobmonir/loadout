tool: cryptool
tags:
- m07
- crypto
- hash
exam_hint: 'CrypTool is an open-source e-learning platform for cryptography and cryptanalysis, providing interactive tools for learning and experimenting with cryptographic algorithms. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://www.cryptool.org/en/
- https://en.wikipedia.org/wiki/CrypTool
actions:
- title: Cryptool --help
  desc: Runs `cryptool` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cryptool --help
- title: Man cryptool
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man cryptool
