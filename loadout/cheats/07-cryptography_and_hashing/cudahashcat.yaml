tool: cudahashcat
tags:
- m07
- crypto
- hash
exam_hint: 'CudaHashcat is a GPU-accelerated password recovery tool, supporting a wide range of hash algorithms for fast brute-force and dictionary attacks. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://hashcat.net/wiki/doku.php?id=hashcat
- https://www.kali.org/tools/hashcat/
actions:
- title: Cudahashcat64.bin -m 0
  desc: Runs `cudahashcat64.bin` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cudahashcat64.bin -m 0 -a 0 hashes.txt wordlist.txt
- title: Cudahashcat64.bin -m 1000
  desc: Runs `cudahashcat64.bin` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cudahashcat64.bin -m 1000 -a 3 hashes.txt ?a?a?a?a?a
