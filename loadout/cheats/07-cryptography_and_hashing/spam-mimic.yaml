tool: spam-mimic
tags:
- m07
- crypto
- hash
exam_hint: 'A steganography tool that encodes hidden messages as innocent-looking spam email text to evade content inspection. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Spam-mimic --help
  desc: Runs `spam-mimic` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: spam-mimic --help
- title: Spam-mimic -h
  desc: Runs `spam-mimic` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: spam-mimic -h
