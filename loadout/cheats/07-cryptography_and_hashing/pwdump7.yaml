tool: pwdump7
tags:
- m07
- crypto
- hash
exam_hint: 'pwdump7 is a Windows password hash extraction tool that reads the Security Account Manager (SAM) database and extracts LM and NTLM password hashes. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Pwdump7 --help
  desc: Runs `pwdump7` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: pwdump7 --help
- title: Man pwdump7
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man pwdump7
