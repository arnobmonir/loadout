tool: openssl
tags:
- m07
- crypto
- hash
exam_hint: 'OpenSSL is a robust, commercial-grade, and full-featured toolkit for the Transport Layer Security (TLS) and Secure Sockets Layer (SSL) protocols. It is also a general-purpose cryptography library providing various cryptographic functions used in conjunction with authentication, encryption, and hashing. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. openssl is a comprehensive cryptography toolkit supporting'
docs:
- https://www.openssl.org/source/openssl-1.1.1.tar.gz
actions:
- title: Apt-get install
  desc: Runs `sudo` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: sudo apt-get install openssl
- title: Dnf install
  desc: Runs `sudo` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: sudo dnf install openssl
- title: Brew install openssl
  desc: Runs `brew` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: brew install openssl
- title: Wget https://www.openssl.org/source/openssl-1.1.1.tar.gz
  desc: Runs `wget` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: wget https://www.openssl.org/source/openssl-1.1.1.tar.gz
- title: Tar xzf openssl-1.1.1.tar.gz
  desc: Runs `tar` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: tar xzf openssl-1.1.1.tar.gz
- title: Cd openssl-1.1.1
  desc: Runs `cd` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cd openssl-1.1.1
- title: Config
  desc: Runs `./config` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: ./config
- title: Make
  desc: Runs `make` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: make
- title: Make install
  desc: Runs `sudo` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: sudo make install
- title: Openssl genrsa -out
  desc: Runs `openssl` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: openssl genrsa -out private.key 2048
- title: Openssl version
  desc: Runs `openssl` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: openssl version
- title: Apt install
  desc: Runs `sudo` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: 'sudo apt install openssl # Debian/Ubuntu'
- title: Openssl COMMAND [options]
  desc: Runs `openssl` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: openssl COMMAND [options]
- title: 'Common Commands:'
  desc: Runs `Common` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: 'Common Commands:'
