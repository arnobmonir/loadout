tool: h-crypt
tags:
- m07
- crypto
- hash
exam_hint: 'H-Crypt is an advanced crypting tool used by cybercriminals to obfuscate and encrypt malicious executables, making them undetectable by traditional antivirus (AV) solutions. Unlike basic crypters, H-Crypt employs polymorphic encryption, anti-sandboxing, and memory injection techniques to bypass security measures. ### Key Features ✔ Polymorphic Encryption – Changes the malware’s signature with each Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack '
docs: []
actions:
- title: H-crypt --help
  desc: Runs `h-crypt` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: h-crypt --help
- title: H-crypt -h
  desc: Runs `h-crypt` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: h-crypt -h
