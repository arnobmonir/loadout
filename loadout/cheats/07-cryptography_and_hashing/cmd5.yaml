tool: cmd5
tags:
- m07
- crypto
- hash
exam_hint: 'Cmd5 is an online MD5 hash lookup and cracking tool, used to identify plaintext values for given MD5 hashes by searching a large database of known hashes. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://www.cmd5.org/
- https://en.wikipedia.org/wiki/MD5
actions:
- title: Cmd5 --help
  desc: Runs `cmd5` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: cmd5 --help
- title: Man cmd5
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man cmd5
