tool: quickcrypto
tags:
- m07
- crypto
- hash
exam_hint: 'A Windows steganography and encryption tool for hiding and encrypting files within other files and images. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Quickcrypto --help
  desc: Runs `quickcrypto` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: quickcrypto --help
- title: Man quickcrypto
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man quickcrypto
