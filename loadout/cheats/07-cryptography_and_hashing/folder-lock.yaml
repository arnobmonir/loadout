tool: folder-lock
tags:
- m07
- crypto
- hash
exam_hint: 'A file encryption and hiding application that locks files and folders behind passwords to protect sensitive data. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Folder-lock --help
  desc: Runs `folder-lock` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: folder-lock --help
- title: Folder-lock -h
  desc: Runs `folder-lock` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: folder-lock -h
