tool: gpg
tags:
- m07
- crypto
- hash
exam_hint: 'GPG — CEH 07 module tool. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://www.gpg4win.org/
- https://gnupg.org/
actions:
- title: Apt install gnupg
  desc: Runs `apt` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: apt install gnupg
