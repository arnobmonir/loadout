tool: ssuite-picsel
tags:
- m07
- crypto
- hash
exam_hint: 'A portable steganography tool for hiding text messages inside image files using cryptographic techniques. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Ssuite-picsel --help
  desc: Runs `ssuite-picsel` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: ssuite-picsel --help
- title: Ssuite-picsel -h
  desc: Runs `ssuite-picsel` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: ssuite-picsel -h
