tool: stegj
tags:
- m07
- crypto
- hash
exam_hint: 'A Java-based steganography tool for hiding files inside image and audio files using various encoding algorithms. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs: []
actions:
- title: Stegj --help
  desc: Runs `stegj` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: stegj --help
- title: Man stegj
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man stegj
