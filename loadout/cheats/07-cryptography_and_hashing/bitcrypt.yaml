tool: bitcrypt
tags:
- m07
- crypto
- hash
exam_hint: 'BitCrypt is a file encryption tool that uses strong cryptographic algorithms to protect files and folders, supporting secure data storage and sharing. Strategy: Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins.'
docs:
- https://www.softpedia.com/get/Security/Encrypting/BitCrypt.shtml
- https://en.wikipedia.org/wiki/File_encryption
actions:
- title: Bitcrypt --help
  desc: Runs `bitcrypt` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: bitcrypt --help
- title: Man bitcrypt
  desc: Runs `man` as part of the CEH workflow. Identify hash mode (hashcat -m / john format); start targeted wordlists and rules; crack service accounts before domain admins. Authorized lab targets only.
  command: man bitcrypt
