tool: kubernetes
tags:
- m10
- cloud
- iot
exam_hint: 'Kubernetes is an open-source container orchestration platform that automates the deployment, scaling, and management of containerized applications. It provides powerful features for managing containerized workloads across clusters of machines. Strategy: Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs.'
docs:
- https://github.com/kubernetes/minikube/releases/download/latest/minikube-linux-amd64
actions:
- title: Apt-get install
  desc: Runs `sudo` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: sudo apt-get install -y kubectl
- title: Curl -LO https://github.com/kubernetes/minikube/releases/dow
  desc: Runs `curl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: curl -LO https://github.com/kubernetes/minikube/releases/download/latest/minikube-linux-amd64
- title: Install minikube-linux-amd64
  desc: Runs `sudo` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: sudo install minikube-linux-amd64 /usr/local/bin/minikube
- title: Apt-get install (2)
  desc: Runs `sudo` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: sudo apt-get install -y kubeadm kubelet kubectl
- title: Kubectl cluster-info
  desc: Runs `kubectl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: kubectl cluster-info
- title: Kubectl get nodes
  desc: Runs `kubectl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: kubectl get nodes
- title: Kubectl apply -f
  desc: Runs `kubectl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: kubectl apply -f deployment.yaml
- title: Kubectl create deployment
  desc: Runs `kubectl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: kubectl create deployment nginx --image=nginx
- title: Kubectl get pods
  desc: Runs `kubectl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: kubectl get pods
- title: Kubectl get pods (2)
  desc: Runs `kubectl` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: kubectl get pods -A
