tool: cloudtrail
tags:
- m10
- cloud
- iot
exam_hint: 'AWS CloudTrail is a web service that records AWS API calls for your account and delivers log files to an Amazon S3 bucket. CloudTrail provides visibility into user activity by recording interactions with your AWS infrastructure and services. Strategy: Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs.'
docs: []
actions:
- title: 1. AWS Console
  desc: Runs `1.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 1. AWS Console → CloudTrail
- title: 2. Create new
  desc: Runs `2.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 2. Create new trail
- title: 3. Specify S3
  desc: Runs `3.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 3. Specify S3 bucket for logs
- title: 4. Choose regions
  desc: Runs `4.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 4. Choose regions (multi-region recommended)
- title: 5. Enable log
  desc: Runs `5.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 5. Enable log file validation
- title: 6. Add CloudWatch
  desc: Runs `6.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 6. Add CloudWatch Logs
- title: 7. Enable SNS
  desc: Runs `7.` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: 7. Enable SNS notifications
- title: '- Athena for'
  desc: Runs `-` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: '- Athena for SQL queries'
- title: '- CloudWatch Logs'
  desc: Runs `-` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: '- CloudWatch Logs Insights'
- title: '- Third-party SIEM'
  desc: Runs `-` as part of the CEH workflow. Enumerate public buckets and IAM misconfigs first; scan container images for known CVEs; segment IoT devices from corp VLANs. Authorized lab targets only.
  command: '- Third-party SIEM integration'
