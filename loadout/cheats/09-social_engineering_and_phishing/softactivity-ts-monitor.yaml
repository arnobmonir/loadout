tool: softactivity-ts-monitor
tags:
- m09
- social
- phishing
exam_hint: 'Terminal Server version of SoftActivity Monitor for monitoring RDS/Citrix session activity. Strategy: Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.'
docs: []
actions:
- title: Echo 'Open SoftActivity
  desc: Runs `echo` as part of the CEH workflow. Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.
  command: echo 'Open SoftActivity TS Monitor per CEH lab procedure — configure target {ip} in isolated environment.'
