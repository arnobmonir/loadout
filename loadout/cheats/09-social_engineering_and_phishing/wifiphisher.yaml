tool: wifiphisher
tags:
- m09
- social
- phishing
exam_hint: 'Wifiphisher is a security testing tool that performs automated phishing attacks against WiFi networks. It creates fake WiFi access points and uses SSL stripping to transparently capture credentials from unsuspecting users. Strategy: Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.'
docs: []
actions:
- title: Wifiphisher --help
  desc: Runs `sudo` as part of the CEH workflow. Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.
  command: sudo wifiphisher --help
