tool: usb-monitor
tags:
- m09
- social
- phishing
exam_hint: 'USB port monitoring tool that logs all USB device connections and activity on Windows systems. Strategy: Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.'
docs: []
actions:
- title: Usb-monitor --help
  desc: Runs `usb-monitor` as part of the CEH workflow. Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.
  command: usb-monitor --help
- title: Usb-monitor -h
  desc: Runs `usb-monitor` as part of the CEH workflow. Scope recipient lists and landing pages; use isolated infra; track opens/clicks only within written authorization.
  command: usb-monitor -h
