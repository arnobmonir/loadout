tool: icacls
tags:
- m11
- network
- cli
exam_hint: 'A Windows command-line tool for viewing and modifying file and directory access control lists (ACLs). Used in privilege escalation. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Icacls --help
  desc: Runs `icacls` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: icacls --help
- title: Man icacls
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man icacls
