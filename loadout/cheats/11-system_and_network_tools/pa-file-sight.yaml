tool: pa-file-sight
tags:
- m11
- network
- cli
exam_hint: 'PA File Sight (by Power Admin) is a real-time file activity monitoring tool for Windows servers and file shares. It monitors and logs all file access, creation, modification, deletion, and permission change events, providing visibility into who accessed or modified which files and when. A key security use case is ransomware detection: PA File Sight can detect the mass file modification and renamin Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.poweradmin.com/products/file-sight/
actions:
- title: Pa-file-sight --help
  desc: Runs `pa-file-sight` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: pa-file-sight --help
- title: Pa-file-sight -h
  desc: Runs `pa-file-sight` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: pa-file-sight -h
