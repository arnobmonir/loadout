tool: stackhawk
tags:
- m11
- network
- cli
exam_hint: 'StackHawk is a modern application security platform focused on dynamic scanning (DAST) for web applications and APIs. It''s designed to integrate seamlessly into development pipelines and provides actionable vulnerability intelligence. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Stackhawk --help
  desc: Runs `stackhawk` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: stackhawk --help
- title: Man stackhawk
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man stackhawk
