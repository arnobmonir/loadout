tool: revo-uninstaller-pro
tags:
- m11
- network
- cli
exam_hint: 'Revo Uninstaller Pro (by VS Revo Group) is an advanced Windows application removal tool that goes beyond the standard Windows uninstaller by performing a deep scan for leftover files, folders, and registry keys after the built-in uninstaller runs. It features a "Forced Uninstall" mode that can remove stubborn programs (including malware that has no standard uninstaller) by scanning for all associa Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.revouninstaller.com/
actions:
- title: RevoUninstallerPro.exe /uninstall "Program
  desc: Runs `RevoUninstallerPro.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: RevoUninstallerPro.exe /uninstall "Program Name"
- title: RevoUninstallerPro.exe /sc /path:"C:\path\to\program.exe"
  desc: Runs `RevoUninstallerPro.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: RevoUninstallerPro.exe /sc /path:"C:\path\to\program.exe"
