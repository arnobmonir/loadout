tool: chameleon-startup-manager
tags:
- m11
- network
- cli
exam_hint: 'Chameleon Startup Manager is a Windows startup management utility that provides comprehensive visibility and control over all programs and processes that automatically start with Windows. It covers registry autorun keys, startup folders, services, scheduled tasks, and browser add-ons. The tool allows enabling, disabling, or deleting startup items and provides detailed information about each entry  Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://docs.microsoft.com/en-us/sysinternals/downloads/autoruns
- https://attack.mitre.org/techniques/T1547/001/
actions:
- title: Get-CimInstance Win32_StartupCommand
  desc: Runs `Get-CimInstance` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-CimInstance Win32_StartupCommand | Select Name, Command, User, Location
