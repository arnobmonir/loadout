tool: mv
tags:
- m11
- network
- cli
exam_hint: 'mv (move) is a command-line utility that moves or renames files and directories. Can relocate files across directories or rename them in place. Essential for file organization and system administration. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Mv --version
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv --version
- title: Mv oldname.txt newname.txt
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv oldname.txt newname.txt
- title: Mv /tmp/file.txt /tmp/renamed.txt
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv /tmp/file.txt /tmp/renamed.txt
- title: Mv file.txt file.txt.bak
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv file.txt file.txt.bak
- title: Mv config old_config
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv config old_config
- title: For f in
  desc: Runs `for` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: for f in *.log; do mv "$f" "${f%.log}.bak"; done
- title: Mv file.txt /backup/
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv file.txt /backup/
- title: Mv *.jpg /images/
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv *.jpg /images/
- title: Mv file.txt /new/location/newname.txt
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv file.txt /new/location/newname.txt
- title: Mv project/ /home/user/projects/
  desc: Runs `mv` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: mv project/ /home/user/projects/
