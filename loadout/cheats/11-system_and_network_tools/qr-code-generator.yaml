tool: qr-code-generator
tags:
- m11
- network
- cli
exam_hint: 'QR Code Generator — CEH 11 module tool. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Qr-code-generator --help
  desc: Runs `qr-code-generator` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: qr-code-generator --help
- title: Qr-code-generator -h
  desc: Runs `qr-code-generator` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: qr-code-generator -h
