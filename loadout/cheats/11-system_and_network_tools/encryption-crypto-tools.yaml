tool: encryption-crypto-tools
tags:
- m11
- network
- cli
exam_hint: 'Encryption_Crypto_Tools — CEH 11 module tool. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs:
- https://www.gpg4win.org/
- https://gnupg.org/
- https://www.nitrokey.com/
actions:
- title: Apt-get install gnupg
  desc: Runs `apt-get` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: apt-get install gnupg
- title: Pip install pycryptodomex
  desc: Runs `pip` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: pip install pycryptodomex
