tool: stream-detector
tags:
- m11
- network
- cli
exam_hint: 'A Windows tool for detecting and managing NTFS Alternate Data Streams hidden within files. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Stream-detector --help
  desc: Runs `stream-detector` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: stream-detector --help
- title: Stream-detector -h
  desc: Runs `stream-detector` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: stream-detector -h
