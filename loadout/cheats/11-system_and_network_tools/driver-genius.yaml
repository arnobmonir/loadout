tool: driver-genius
tags:
- m11
- network
- cli
exam_hint: 'Driver Genius (by Driver-Soft) is a professional Windows driver management suite that provides driver update, backup, restore, and uninstall capabilities. It maintains a large driver database (100,000+ drivers) and automatically detects outdated, missing, or damaged drivers. Driver Genius''s security relevance lies in ensuring driver-level components are current and verified, closing vulnerability  Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.driver-soft.com/
actions:
- title: Get-WmiObject Win32_PnPSignedDriver
  desc: Runs `Get-WmiObject` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-WmiObject Win32_PnPSignedDriver | Select DeviceName, DriverVersion, DriverDate | Format-Table
- title: Get-WmiObject Win32_PnPSignedDriver (2)
  desc: Runs `Get-WmiObject` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-WmiObject Win32_PnPSignedDriver | Where {$_.IsSigned -eq $false}
