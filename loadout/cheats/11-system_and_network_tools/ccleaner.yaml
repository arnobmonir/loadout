tool: ccleaner
tags:
- m11
- network
- cli
exam_hint: 'A Windows system optimization and privacy tool that deletes browser history, temporary files, and traces of computer activity. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Ccleaner --help
  desc: Runs `ccleaner` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ccleaner --help
- title: Man ccleaner
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man ccleaner
