tool: sed
tags:
- m11
- network
- cli
exam_hint: 'sed (stream editor) is a non-interactive command-line text editor that processes input line-by-line and outputs transformed text. sed uses patterns and editing commands to perform text substitution, deletion, insertion, and transformation on files or streams. It''s essential for automated text processing and data transformation pipelines. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Sed --version
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sed --version
- title: Apt-get install
  desc: Runs `sudo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sudo apt-get install sed
- title: Yum install
  desc: Runs `sudo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sudo yum install sed
- title: Brew install gnu-sed
  desc: Runs `brew` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: brew install gnu-sed
- title: Sed 's/old/new/' file.txt
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sed 's/old/new/' file.txt
- title: Sed 's/old/new/g' file.txt
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sed 's/old/new/g' file.txt
- title: Sed -i 's/old/new/g'
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'sed -i ''s/old/new/g'' file.txt # In-place edit'
- title: Sed 's/OLD/new/i' file.txt
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sed 's/OLD/new/i' file.txt
- title: Sed '5s/old/new/' file.txt
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sed '5s/old/new/' file.txt
- title: Sed 's/old/new/' file.txt (2)
  desc: Runs `sed` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: sed 's/old/new/' file.txt > newfile.txt
