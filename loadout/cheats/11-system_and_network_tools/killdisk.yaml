tool: killdisk
tags:
- m11
- network
- cli
exam_hint: 'A professional disk eraser for HDDs, SSDs, and USB drives supporting multiple military-grade data sanitization standards. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Killdisk --help
  desc: Runs `killdisk` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: killdisk --help
- title: Man killdisk
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man killdisk
