tool: accesschk
tags:
- m11
- network
- cli
exam_hint: 'A Sysinternals utility for checking access permissions on files, directories, registry keys, processes, services, and tokens. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Accesschk --help
  desc: Runs `accesschk` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: accesschk --help
- title: Man accesschk
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man accesschk
