tool: get-objectacl
tags:
- m11
- network
- cli
exam_hint: 'A PowerView PowerShell cmdlet for enumerating Active Directory object ACLs to identify privilege escalation paths. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Get-objectacl --help
  desc: Runs `get-objectacl` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: get-objectacl --help
- title: Get-objectacl -h
  desc: Runs `get-objectacl` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: get-objectacl -h
