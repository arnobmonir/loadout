tool: get-userproperty
tags:
- m11
- network
- cli
exam_hint: 'A PowerView PowerShell cmdlet for enumerating AD user account properties for reconnaissance and privilege escalation. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Get-userproperty --help
  desc: Runs `get-userproperty` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: get-userproperty --help
- title: Get-userproperty -h
  desc: Runs `get-userproperty` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: get-userproperty -h
