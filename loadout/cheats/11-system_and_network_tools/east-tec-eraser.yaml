tool: east-tec-eraser
tags:
- m11
- network
- cli
exam_hint: 'A secure data erasure tool for permanently wiping files, folders, browser history, and internet tracks from Windows systems. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: East-tec-eraser --help
  desc: Runs `east-tec-eraser` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: east-tec-eraser --help
- title: East-tec-eraser -h
  desc: Runs `east-tec-eraser` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: east-tec-eraser -h
