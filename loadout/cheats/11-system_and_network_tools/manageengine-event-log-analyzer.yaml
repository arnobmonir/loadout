tool: manageengine-event-log-analyzer
tags:
- m11
- network
- cli
exam_hint: 'ManageEngine Event Log Analyzer is an IT compliance and log management solution that collects, analyses, and archives Windows Event Logs, Unix/Linux syslog, application logs, and network device logs from across the environment. It provides real-time event correlation, predefined compliance reports (PCI DSS, HIPAA, SOX, GDPR), and threat detection through correlation rules. During malware incidents Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- http://<server
- https://www.manageengine.com/products/eventlog/
- https://docs.microsoft.com/en-us/windows/security/threat-protection/auditing/
actions:
- title: Get-WinEvent -FilterHashtable @{LogName='Security';
  desc: Runs `Get-WinEvent` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-WinEvent -FilterHashtable @{LogName='Security'; Id=4625} | Select TimeCreated, Message
- title: Get-WinEvent -FilterHashtable @{LogName='System';
  desc: Runs `Get-WinEvent` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-WinEvent -FilterHashtable @{LogName='System'; Id=7045} | Select TimeCreated, Message
