tool: monit
tags:
- m11
- network
- cli
exam_hint: 'Monit is a free, open-source monitoring utility for Unix/Linux systems that monitors and manages daemons, processes, files, directories, filesystems, network connections, and system resources. It performs automatic remediation (restart failed services, alert on resource abuse). From a security perspective, Monit can monitor critical security daemon uptime (antivirus, IDS, firewall processes), aler Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- http://localhost:2812
- https://mmonit.com/monit/
- https://mmonit.com/monit/documentation/
actions:
- title: Apt-get install
  desc: Runs `sudo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'sudo apt-get install monit # Debian/Ubuntu'
- title: Yum install
  desc: Runs `sudo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'sudo yum install monit # CentOS/RHEL'
- title: Check process clamd
  desc: Runs `check` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: check process clamd with pidfile /var/run/clamd.pid
- title: Start program =
  desc: Runs `start` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: start program = "/etc/init.d/clamd start"
- title: Stop program =
  desc: Runs `stop` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: stop program = "/etc/init.d/clamd stop"
- title: If not running
  desc: Runs `if` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: if not running then restart
- title: If cpu >
  desc: Runs `if` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: if cpu > 90% then alert
- title: Check file sudoers
  desc: Runs `check` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: check file sudoers with path /etc/sudoers
- title: If failed checksum
  desc: Runs `if` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: if failed checksum then alert
- title: Check host google.com
  desc: Runs `check` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: check host google.com with address google.com
