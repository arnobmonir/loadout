tool: solarwinds-open-port-scanner
tags:
- m11
- network
- cli
exam_hint: 'SolarWinds Open Port Scanner (part of the SolarWinds Engineer''s Toolset and also available as a free standalone tool) is a Windows GUI-based TCP port scanner that rapidly identifies open ports on target hosts or IP ranges. It supports single IP, IP range, and subnet scanning, and displays results in a clear colour-coded interface. During security assessments and malware investigations, it is used  Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.solarwinds.com/free-tools/port-scanner
- https://nmap.org/
actions:
- title: Nmap -sV -p
  desc: Runs `nmap` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'nmap -sV -p 1-1000 192.168.1.10 # Nmap service version scan'
- title: Masscan 192.168.1.0/24 -p1-65535
  desc: Runs `masscan` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'masscan 192.168.1.0/24 -p1-65535 # masscan for high-speed scanning'
