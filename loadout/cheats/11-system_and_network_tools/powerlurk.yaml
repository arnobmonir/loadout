tool: powerlurk
tags:
- m11
- network
- cli
exam_hint: 'A PowerShell toolkit for registering WMI event subscriptions for persistence and triggering malicious actions based on system events. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Powerlurk --help
  desc: Runs `powerlurk` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: powerlurk --help
- title: Man powerlurk
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man powerlurk
