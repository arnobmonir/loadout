tool: alternatestreamview
tags:
- m11
- network
- cli
exam_hint: 'NirSoft utility that scans NTFS drives and lists all Alternate Data Streams including their size and data. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Alternatestreamview --help
  desc: Runs `alternatestreamview` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: alternatestreamview --help
- title: Man alternatestreamview
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man alternatestreamview
