tool: dnsstuff
tags:
- m11
- network
- cli
exam_hint: 'DNSstuff is an online network information and DNS lookup service that provides a comprehensive suite of DNS tools, email diagnostics, blacklist checking, WHOIS lookups, and traceroute utilities. During security investigations, DNSstuff is used to check whether a domain or IP address is listed on spam/malware blacklists (RBL/DNSBL), investigate DNS configurations, trace email delivery paths, and lo Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.dnsstuff.com/
actions:
- title: Nslookup 4.3.2.1.zen.spamhaus.org
  desc: Runs `nslookup` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: nslookup 4.3.2.1.zen.spamhaus.org
- title: Dig 4.3.2.1.zen.spamhaus.org A
  desc: Runs `dig` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'dig 4.3.2.1.zen.spamhaus.org A # returns 127.0.0.x if listed'
- title: Nslookup -type=ANY suspicious-domain.com
  desc: Runs `nslookup` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: nslookup -type=ANY suspicious-domain.com
- title: Dig ANY suspicious-domain.com
  desc: Runs `dig` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: dig ANY suspicious-domain.com
- title: Nslookup 203.0.113.50
  desc: Runs `nslookup` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: nslookup 203.0.113.50
- title: Dig -x 203.0.113.50
  desc: Runs `dig` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: dig -x 203.0.113.50
