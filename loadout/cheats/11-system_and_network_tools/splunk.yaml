tool: splunk
tags:
- m11
- network
- cli
exam_hint: 'Splunk is the industry-leading SIEM (Security Information and Event Management) and data analytics platform for collecting, indexing, searching, and visualising machine data from any source. In cybersecurity, Splunk is deployed as the centralised log aggregation and threat detection platform, correlating events from endpoints (Sysmon, Windows Event Logs), network devices, firewalls, IDS/IPS, and a Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.splunk.com/
- https://splunkbase.splunk.com/app/3435/
- https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/UnderstandingSPLsyntax
actions:
- title: Index=wineventlog EventCode=4625
  desc: Runs `index=wineventlog` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: index=wineventlog EventCode=4625
- title: Run command 2
  desc: Runs `|` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '| stats count by user, src_ip'
- title: Run command 3
  desc: Runs `|` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '| sort -count'
- title: Index=wineventlog EventCode=4624 LogonType=9
  desc: Runs `index=wineventlog` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: index=wineventlog EventCode=4624 LogonType=9
- title: Run command 5
  desc: Runs `|` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '| where match(CommandLine, "(?i)(sekurlsa|lsadump|mimikatz)")'
- title: Index=wineventlog EventCode=4104
  desc: Runs `index=wineventlog` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: index=wineventlog EventCode=4104
- title: Run command 7
  desc: Runs `|` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '| where match(ScriptBlockText, "(?i)(-enc|-encodedcommand|frombase64)")'
- title: Index=wineventlog EventCode=4698
  desc: Runs `index=wineventlog` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: index=wineventlog EventCode=4698
- title: Run command 9
  desc: Runs `|` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '| table _time, host, user, TaskName, TaskContent'
- title: Index=network dest_port=443 src_ip=10.0.0.0/8
  desc: Runs `index=network` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: index=network dest_port=443 src_ip=10.0.0.0/8
