tool: system-explorer
tags:
- m11
- network
- cli
exam_hint: 'System Explorer is a free Windows system monitoring utility that provides an integrated view of running processes, threads, modules, handles, network connections, startup programs, scheduled tasks, services, drivers, and installed programs—all from a single interface. Unlike Sysinternals tools which cover individual aspects, System Explorer provides a unified dashboard with VirusTotal integration  Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://systemexplorer.net/
actions:
- title: 1. Open SystemExplorer.exe
  desc: Runs `1.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 1. Open SystemExplorer.exe
- title: '2. Processes tab:'
  desc: Runs `2.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '2. Processes tab: view all processes with VT scan option'
- title: '3. Startup Manager:'
  desc: Runs `3.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '3. Startup Manager: review all boot persistence locations'
- title: '4. Network tab:'
  desc: Runs `4.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '4. Network tab: live TCP/UDP connection list per process'
- title: '5. Services tab:'
  desc: Runs `5.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '5. Services tab: view/stop suspicious Windows services'
- title: '6. Handles tab:'
  desc: Runs `6.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '6. Handles tab: file/registry handles per process'
- title: '7. Modules tab:'
  desc: Runs `7.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '7. Modules tab: loaded DLLs and their trust status'
- title: Right-click process >
  desc: Runs `Right-click` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Right-click process > Check on VirusTotal
- title: Tools > System
  desc: Runs `Tools` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Tools > System Snapshot > Compare snapshots
