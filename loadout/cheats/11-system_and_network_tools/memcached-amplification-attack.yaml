tool: memcached-amplification-attack
tags:
- m11
- network
- cli
exam_hint: 'Memcached Amplification Attack is an extremely potent distributed denial-of-service vector that exploits public Memcached servers (caching systems) exposed on the internet. Attackers send small spoofed requests to Memcached servers, receiving massive responses (up to 1 million bytes) directed at victims. With amplification factors exceeding 10,000x, Memcached amplification represents one of the mo Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs: []
actions:
- title: 'Amplification Power:'
  desc: Runs `Amplification` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'Amplification Power:'
- title: '- Query size:'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Query size: 15 bytes'
- title: '- Response size:'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Response size: 134,000+ bytes'
- title: '- Amplification factor:'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Amplification factor: 8,900x'
- title: '- Practical attack:'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Practical attack: 1,500 Gbps from single server'
- title: 'Impact:'
  desc: Runs `Impact:` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: 'Impact:'
- title: '- Largest documented'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Largest documented DDoS attacks used Memcached'
- title: '- February 2018:'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- February 2018: 1.35 Tbps attack on GitHub'
- title: '- Few servers'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Few servers needed for massive attacks'
- title: '- Easy to'
  desc: Runs `-` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '- Easy to execute'
