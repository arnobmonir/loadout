tool: anvir-task-manager
tags:
- m11
- network
- cli
exam_hint: 'AnVir Task Manager is a Windows system utility that replaces the standard Task Manager with a feature-rich interface for managing processes, startup programs, services, and browser extensions. It integrates with VirusTotal and provides risk scores for running processes and startup entries, making it useful for quickly identifying suspicious or malicious software on a system. It can detect hidden p Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.anvir.com/
actions:
- title: '1. Processes tab:'
  desc: Runs `1.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '1. Processes tab: risk-scored process list with VT integration'
- title: '2. Startup Manager:'
  desc: Runs `2.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '2. Startup Manager: comprehensive view of all startup locations'
- title: '3. Browser plugins:'
  desc: Runs `3.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '3. Browser plugins: detect and manage extensions installed by malware'
- title: '4. Services: view/stop'
  desc: Runs `4.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '4. Services: view/stop suspicious Windows services'
- title: '5. Real-time alerts:'
  desc: Runs `5.` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: '5. Real-time alerts: notification when a new startup entry is added'
- title: Right-click any process
  desc: Runs `Right-click` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Right-click any process > Check on VirusTotal
- title: Options > Search
  desc: Runs `Options` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Options > Search for Hidden Processes
