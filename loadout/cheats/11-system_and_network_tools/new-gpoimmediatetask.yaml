tool: new-gpoimmediatetask
tags:
- m11
- network
- cli
exam_hint: 'New-GPOImmediateTask is a PowerShell function from the PowerSploit framework used to abuse Group Policy Objects by creating an immediate scheduled task through GPO for code execution and privilege escalation. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: New-GPOImmediateTask -TaskName "Task"
  desc: Runs `New-GPOImmediateTask` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: New-GPOImmediateTask -TaskName "Task" -GPODisplayName "GPO" -Command "cmd.exe" -CommandArguments "/c whoami"
