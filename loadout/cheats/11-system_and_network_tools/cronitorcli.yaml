tool: cronitorcli
tags:
- m11
- network
- cli
exam_hint: 'CronitorCLI is a command-line tool and monitoring service for cron jobs, scheduled tasks, and background processes. It sends real-time notifications when scheduled jobs start, complete, or fail, and detects "silent failures" where a cron job stops running without producing errors. From a security perspective, monitoring scheduled tasks is critical for detecting malware persistence mechanisms—malwa Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://cronitor.io/install
- https://cronitor.io/app
- https://cronitor.io/
actions:
- title: Curl https://cronitor.io/install
  desc: Runs `curl` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: curl https://cronitor.io/install | bash
- title: Brew install cronitor/taps/cronitor
  desc: Runs `brew` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: brew install cronitor/taps/cronitor
- title: Cronitor exec <monitor-key>
  desc: Runs `cronitor` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: cronitor exec <monitor-key> /path/to/script.sh
- title: Cronitor discover
  desc: Runs `cronitor` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: cronitor discover
- title: Cronitor select
  desc: Runs `cronitor` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: cronitor select
- title: Cronitor sync
  desc: Runs `cronitor` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: cronitor sync
- title: Cronitor exec my-backup-job
  desc: Runs `cronitor` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: cronitor exec my-backup-job -- /opt/backup.sh
