tool: fsutil
tags:
- m11
- network
- cli
exam_hint: 'A Windows command-line utility for file system management. Attackers use it to delete USN journal entries to cover tracks. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Wevtutil cl Security
  desc: Runs `wevtutil` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: wevtutil cl Security
