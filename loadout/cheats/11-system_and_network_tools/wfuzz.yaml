tool: wfuzz
tags:
- m11
- network
- cli
exam_hint: 'wfuzz is an open-source web application fuzzer for discovering resources, vulnerabilities, and misconfigurations by sending crafted requests. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs:
- http://target.com/FUZZ
- https://wfuzz.readthedocs.io/en/latest/
- https://owasp.org/www-community/Fuzzing
actions:
- title: Wfuzz -c -w
  desc: Runs `wfuzz` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: wfuzz -c -w wordlist.txt --hc 404 http://target.com/FUZZ
