tool: dban
tags:
- m11
- network
- cli
exam_hint: 'Darik''s Boot and Nuke — a self-contained boot disk that securely wipes hard drives using DoD 5220.22-M and other standards. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Dban --help
  desc: Runs `dban` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: dban --help
- title: Man dban
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man dban
