tool: netwrix-service-monitor
tags:
- m11
- network
- cli
exam_hint: 'Netwrix Service Monitor (also offered as a free tool) continuously monitors the status of Windows services across multiple computers and automatically sends email alerts when a service stops, crashes, or is disabled. From a security perspective, malware often terminates or disables critical security services (antivirus, Windows Defender, firewall, event log service) to facilitate its operations wi Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.netwrix.com/free_service_monitor.html
actions:
- title: Echo 'Open Netwrix
  desc: Runs `echo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: echo 'Open Netwrix Service Monitor per CEH lab procedure — configure target {ip} in isolated environment.'
