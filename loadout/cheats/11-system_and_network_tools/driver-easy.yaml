tool: driver-easy
tags:
- m11
- network
- cli
exam_hint: 'Driver Easy is a Windows driver management tool that scans installed hardware, detects outdated or missing drivers, and provides one-click driver updates from a large driver database. Like Driver Booster, it helps keep system drivers current to close known driver-level vulnerabilities. It includes driver backup and restore functionality, ensuring that if a driver update causes problems (or is repl Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.drivereasy.com/
actions:
- title: Get-WmiObject Win32_PnPSignedDriver
  desc: Runs `Get-WmiObject` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-WmiObject Win32_PnPSignedDriver | Select DeviceName, DriverVersion, DriverDate | Sort DeviceName
- title: Get-AuthenticodeSignature C:\Windows\System32\drivers\suspic
  desc: Runs `Get-AuthenticodeSignature` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-AuthenticodeSignature C:\Windows\System32\drivers\suspicious.sys
