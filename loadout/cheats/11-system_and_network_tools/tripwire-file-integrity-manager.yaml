tool: tripwire-file-integrity-manager
tags:
- m11
- network
- cli
exam_hint: 'A file integrity monitoring solution that detects unauthorized changes to files, directories, and system configurations. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Echo 'Open Tripwire
  desc: Runs `echo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: echo 'Open Tripwire File Integrity Manager per CEH lab procedure — configure target {ip} in isolated environment.'
