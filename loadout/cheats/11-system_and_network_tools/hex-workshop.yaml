tool: hex-workshop
tags:
- m11
- network
- cli
exam_hint: 'Hex Workshop is a commercial hex editor for Windows that enables users to view, edit, and analyze binary files at the byte level. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs:
- https://www.hexworkshop.com/
- https://en.wikipedia.org/wiki/Hex_editor
actions:
- title: Hex-workshop --help
  desc: Runs `hex-workshop` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: hex-workshop --help
- title: Hex-workshop -h
  desc: Runs `hex-workshop` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: hex-workshop -h
