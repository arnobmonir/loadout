tool: auditpol
tags:
- m11
- network
- cli
exam_hint: 'A Windows command-line tool for managing audit policies. Attackers use it to disable auditing to cover their tracks. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Wevtutil cl Security
  desc: Runs `wevtutil` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: wevtutil cl Security
