tool: extrahop
tags:
- m11
- network
- cli
exam_hint: 'ExtraHop (RevealX NDR) is a network detection and response platform that analyzes wire data in real time to detect suspicious activity, including reconnaissance patterns such as ping sweeps, port scans, and lateral movement attempts. It combines network telemetry, behavioral analytics, and threat intelligence to identify attacker behavior without relying only on endpoint agents. In CEH-relevant sc Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.extrahop.com/platform/revealx/
actions:
- title: Nmap -sS -p
  desc: Runs `nmap` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: nmap -sS -p 1-1000 10.10.10.0/24
- title: Nmap -sn 10.10.10.0/24
  desc: Runs `nmap` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: nmap -sn 10.10.10.0/24
