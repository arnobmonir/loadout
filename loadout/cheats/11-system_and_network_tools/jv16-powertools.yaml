tool: jv16-powertools
tags:
- m11
- network
- cli
exam_hint: 'jv16 PowerTools (by Macecraft Software) is a comprehensive Windows system maintenance and optimisation suite that includes registry cleaner, startup manager, file finder, duplicate finder, history cleaner, and software uninstaller. During malware remediation, jv16 PowerTools is used to clean residual malware registry entries, manage and remove malicious startup programs, find and delete malware-dr Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://www.macecraft.com/
actions:
- title: Get-ItemProperty HKLM:\SOFTWARE\Microsoft\Windows\CurrentVer
  desc: Runs `Get-ItemProperty` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-ItemProperty HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
- title: Get-ItemProperty HKCU:\SOFTWARE\Microsoft\Windows\CurrentVer
  desc: Runs `Get-ItemProperty` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Get-ItemProperty HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
- title: Remove-ItemProperty HKLM:\SOFTWARE\Microsoft\Windows\Current
  desc: Runs `Remove-ItemProperty` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: Remove-ItemProperty HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run -Name "MalwareEntry"
