tool: solarwinds-loggly
tags:
- m11
- network
- cli
exam_hint: 'SolarWinds Loggly is a cloud-based log management and analysis service (formerly Loggly, acquired by SolarWinds) that ingests, indexes, searches, and visualises log data from any source—applications, servers, cloud services, network devices, and containers. It provides real-time search, dashboards, alerts, and anomaly detection across all log sources. Security teams use Loggly as a lightweight clo Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on th'
docs:
- https://logs-01.loggly.com/inputs/<TOKEN
- https://www.loggly.com/
actions:
- title: Curl -H "content-type:text/plain"
  desc: Runs `curl` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: curl -H "content-type:text/plain" -d "Hello Loggly from the command line!" https://logs-01.loggly.com/inputs/<TOKEN>/tag/test/
