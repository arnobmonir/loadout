tool: owasp-zed-attack-proxy-zap
tags:
- m11
- network
- cli
exam_hint: 'OWASP Zed Attack Proxy (ZAP) is a free, open-source web application security scanner and proxy for finding vulnerabilities in web apps. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs:
- https://www.zaproxy.org/
- https://owasp.org/www-project-zap/
actions:
- title: Echo 'Open OWASP
  desc: Runs `echo` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: echo 'Open OWASP Zed Attack Proxy (ZAP) per CEH lab procedure — configure target {ip} in isolated environment.'
