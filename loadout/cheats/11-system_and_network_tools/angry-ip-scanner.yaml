tool: angry-ip-scanner
tags:
- m11
- network
- cli
exam_hint: 'Angry IP Scanner is a fast and user-friendly IP address and port scanner that can scan IP addresses in any range as well as any port in each address. It''s known for its speed and simplicity. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Angry-ip-scanner --help
  desc: Runs `angry-ip-scanner` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: angry-ip-scanner --help
- title: Angry-ip-scanner -h
  desc: Runs `angry-ip-scanner` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: angry-ip-scanner -h
