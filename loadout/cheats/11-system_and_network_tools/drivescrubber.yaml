tool: drivescrubber
tags:
- m11
- network
- cli
exam_hint: 'A tool from iolo for securely erasing hard drive data to prevent data recovery, meeting government-grade standards. Strategy: nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise.'
docs: []
actions:
- title: Drivescrubber --help
  desc: Runs `drivescrubber` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: drivescrubber --help
- title: Man drivescrubber
  desc: Runs `man` as part of the CEH workflow. nc/socat for relays; curl/wget for payload staging; tcpdump/tshark with BPF filters on the pivot interface to limit noise. Authorized lab targets only.
  command: man drivescrubber
