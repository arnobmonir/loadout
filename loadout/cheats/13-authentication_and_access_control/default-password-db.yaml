tool: default-password-db
tags:
- m13
- auth
- ad
exam_hint: 'Default Password DB — CEH 13 module tool. Strategy: Map AD attack paths with BloodHound before spraying; target AS-REP roastable and SPN accounts; respect lockout thresholds.'
docs:
- https://cirt.net/passwords
- https://fortypoundhead.com/tools_dpw.asp
- https://www.defaultpassword.com
actions:
- title: Default-password-db --help
  desc: Runs `default-password-db` as part of the CEH workflow. Map AD attack paths with BloodHound before spraying; target AS-REP roastable and SPN accounts; respect lockout thresholds. Authorized lab targets only.
  command: default-password-db --help
- title: Default-password-db -h
  desc: Runs `default-password-db` as part of the CEH workflow. Map AD attack paths with BloodHound before spraying; target AS-REP roastable and SPN accounts; respect lockout thresholds. Authorized lab targets only.
  command: default-password-db -h
