tool: orbot
tags:
- m14
- mobile
- endpoint
exam_hint: 'Orbot — CEH 14 module tool. Strategy: Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.'
docs:
- https://www.torproject.org/download/#android
actions:
- title: Orbot --help
  desc: Runs `orbot` as part of the CEH workflow. Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.
  command: orbot --help
- title: Man orbot
  desc: Runs `man` as part of the CEH workflow. Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.
  command: man orbot
