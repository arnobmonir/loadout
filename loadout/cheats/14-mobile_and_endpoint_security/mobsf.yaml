tool: mobsf
tags:
- m14
- mobile
- endpoint
exam_hint: 'MobSF (Mobile Security Framework) is an automated, all-in-one mobile application (Android/iOS/Windows) pen-testing, malware analysis, and security assessment framework. Strategy: Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.'
docs:
- https://mobsf.github.io/docs/
- https://github.com/MobSF/Mobile-Security-Framework-MobSF
actions:
- title: Mobsf --help
  desc: Runs `mobsf` as part of the CEH workflow. Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.
  command: mobsf --help
- title: Man mobsf
  desc: Runs `man` as part of the CEH workflow. Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.
  command: man mobsf
