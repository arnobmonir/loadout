tool: mspy
tags:
- m14
- mobile
- endpoint
exam_hint: 'Mobile spyware app for monitoring target iOS/Android devices including calls, messages, GPS, and social media. Strategy: Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.'
docs: []
actions:
- title: Mspy --help
  desc: Runs `mspy` as part of the CEH workflow. Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.
  command: mspy --help
- title: Man mspy
  desc: Runs `man` as part of the CEH workflow. Static APK/IPA analysis before dynamic; use emulators with no prod accounts; endpoint tools only on owned lab devices.
  command: man mspy
